scalar Long

type Product {
    id: ID!
    name: String!
    description: String
    price: Float!
    discount: Float
    point: Int
    inventory: Int!
    thumbnail: String!
    images: [String]
}

type Query {
    findAllProducts: [Product]!
    countProducts: Long!
}
